## 멀티 카운터 생성
```
  복잡한 상태 관리
  chrome개발자도구 redux DevTools 설치하기
```

## 정리
```
  리덕스를 사용해야 하는 이유는 간단하다.
    - 복잡한 상태관리를 해결하기 위한 라이브러리 이다.
    - 상태를 컴포넌트 내부가 아닌 리듀서에서 관리하면 코드가 분리된다.
    - 상태관리를 분리 해둠으로써 컴포넌트를 만들때 뷰에 조금더 집중할 수 있다.
    - 리덕스짱짱맨 외우자 
```